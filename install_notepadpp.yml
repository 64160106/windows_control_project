---
  - name: Copy Notepad++ installer to Windows host
    hosts: windows_servers
    gather_facts: yes
    collections:
      - ansible.windows
    tasks:
      - name: Copy Notepad++ installer to Windows host
        win_copy:
          src: /path/to/notepad++_installer.exe
          dest: C:\temp\notepad++_installer.exe
  